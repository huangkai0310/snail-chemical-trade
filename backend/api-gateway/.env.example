# 复制此文件为 .env，本地开发用
PORT=8080
DATABASE_URL=postgres://postgres:postgres@localhost:5432/snailtrade?sslmode=disable
JWT_SECRET=snail-chemical-trade-dev-secret
REDIS_ADDR=localhost:6379

# migrations 目录：本地相对路径，服务器绝对路径
# 不设置则 fallback 到可执行文件旁边的 migrations/ 目录
MIGRATIONS_DIR=./migrations
